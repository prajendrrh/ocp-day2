# Optional: sync only after ldap-bind-password Secret and ldap-ca ConfigMap exist
# in openshift-config. See ldap-authentication/README.md.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../ldap-authentication/oauth-ldap-idp.yaml
