# MachineConfig (wave 5) then APIServer encryption (wave 20). All paths stay under this
# directory so Argo CD's default Kustomize load restrictions succeed.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - 99-worker-chrony.yaml
  - apiserver-encryption-aesgcm.yaml
